{
  "name": "sharefrom",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "npm run build:only",
    "build:only": "vite build",
    "deploy": "npm run build && wrangler deploy",
    "dev": "npm run format && npm run test && npm run build:only",
    "dev:run": "npm run build && wrangler dev --live-reload",
    "format": "npm run lint -- --fix",
    "lint": "eslint",
    "postinstall": "npm run build:only",
    "prebuild": "npm run lint",
    "prelint": "npm run typecheck",
    "preview": "vite preview",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit && tsc --project worker/tsconfig.json"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20251117.0",
    "@vitest/ui": "4.0.15",
    "eslint": "9.39.1",
    "eslint-config-andrewaylett": "6.1.0",
    "happy-dom": "20.0.10",
    "jsdom": "27.2.0",
    "typescript": "5.9.3",
    "vite": "7.2.2",
    "vitest": "4.0.15",
    "wrangler": "4.47.0"
  },
  "dependencies": {
    "@plausible-analytics/tracker": "0.4.4",
    "@types/qrcode": "1.5.6",
    "qrcode": "1.5.4"
  }
}
